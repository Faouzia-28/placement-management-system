services:
  frontend:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./frontend-deploy/dist:/usr/share/nginx/html:ro
      - ./deploy/nginx.frontend.conf:/etc/nginx/conf.d/default.conf:ro
